"""
CricAuction Backend - Main Application Entry Point

This module initializes and runs the FastAPI application with all configured routes,
middleware, and database setup.
"""

# To run the application:
# 1. Make sure you have installed all dependencies: pip install -r requirements.txt
# 2. Create .env file with proper configuration
# 3. Run: python -m uvicorn app.main:app --reload

# Or simply: uvicorn app.main:app --reload

# The API will be available at http://localhost:8000
# Swagger UI: http://localhost:8000/docs
# ReDoc: http://localhost:8000/redoc
